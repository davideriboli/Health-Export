<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Minimal XML theme — the full Material 3 theming lives in Compose (ui/theme/).
        This theme is required by the system before Compose is active (e.g. splash window).
    -->
    <style name="Theme.HealthExport" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
