package com.healthexport.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary — warm terracotta (Claude's signature accent) ──────────────────
val Primary80      = Color(0xFFFFB59A)   // light mode container text
val Primary40      = Color(0xFFC1522A)   // light mode primary
val Primary90      = Color(0xFFFFDBCF)   // light mode primary container
val Primary10      = Color(0xFF3C0900)   // dark mode on-primary-container
val PrimaryDark80  = Color(0xFFFFB59A)
val PrimaryDark40  = Color(0xFFE8704F)   // dark mode primary (lighter for contrast)

// ── Secondary — warm sand/taupe ────────────────────────────────────────────
val Secondary80    = Color(0xFFE5BFA8)
val Secondary40    = Color(0xFF7C5B47)
val Secondary90    = Color(0xFFFFDBC9)
val Secondary10    = Color(0xFF2E1508)

// ── Tertiary — muted sage (for success / health positive indicators) ───────
val Tertiary80     = Color(0xFFB5CCBA)
val Tertiary40     = Color(0xFF3E6847)
val Tertiary90     = Color(0xFFD1E8D5)
val Tertiary10     = Color(0xFF072010)

// ── Neutral — warm grey-brown for surfaces ────────────────────────────────
val Neutral99      = Color(0xFFFFFBF8)   // background (warm white)
val Neutral95      = Color(0xFFF5EDE7)   // surface variant
val Neutral90      = Color(0xFFE8DDD7)
val Neutral20      = Color(0xFF3C3230)
val Neutral10      = Color(0xFF221A18)

// ── Error ─────────────────────────────────────────────────────────────────
val Error80        = Color(0xFFFFB4AB)
val Error40        = Color(0xFFBA1A1A)
val Error90        = Color(0xFFFFDAD6)
val Error10        = Color(0xFF410002)
